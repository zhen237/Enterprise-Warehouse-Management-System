# 在线演示部署指南（Render 平台）

本文档指导你将企业仓库管理系统部署到 Render 平台，获得可公开访问的在线演示地址。

## 一、部署架构

```
用户浏览器
    │
    ├──→ 前端静态站点 (https://warehouse-frontend.onrender.com)
    │       └── Vue 3 + Vite 构建的静态文件
    │
    └──→ 后端 API 服务 (https://warehouse-backend.onrender.com)
            └── Spring Boot Docker 容器
                    │
                    └──→ MySQL 数据库 (Render 内部网络)
```

## 二、前置条件

- ✅ GitHub 账号（已有：zhen237）
- ✅ 代码已推送到 GitHub 仓库
- ✅ 仓库中包含 `render.yaml` 配置文件

## 三、部署步骤

### 步骤 1：注册 Render 账号

1. 访问 https://render.com
2. 点击 **Sign Up** → 选择 **GitHub** 登录
3. 授权 Render 访问你的 GitHub 仓库

### 步骤 2：创建 Blueprint 部署

1. 进入 Render Dashboard（https://dashboard.render.com）
2. 点击右上角 **New +** → 选择 **Blueprint**
3. 选择你的仓库 `Enterprise-Warehouse-Management-System`
4. Render 会自动检测到 `render.yaml` 文件，显示将创建的服务：
   - `warehouse-db`（MySQL 数据库）
   - `warehouse-backend`（后端 API）
   - `warehouse-frontend`（前端页面）
5. 点击 **Apply** 开始部署

### 步骤 3：等待构建完成

- 数据库：约 1 分钟创建完成
- 后端服务：约 3-5 分钟（首次需要下载 Maven 依赖和 Docker 镜像）
- 前端服务：约 2-3 分钟（npm install + npm build）

在 Dashboard 可以看到每个服务的构建日志，等待状态变为 **Live**（绿色）。

### 步骤 4：验证部署

1. 访问前端地址：`https://warehouse-frontend.onrender.com`
2. 用 `admin / admin123` 登录
3. 确认能看到商品数据（说明后端 API 和数据库连接正常）

## 四、获取部署地址

部署完成后，在 Render Dashboard 中可以看到每个服务的地址：

| 服务 | 地址格式 |
|------|---------|
| 前端页面 | `https://warehouse-frontend.onrender.com` |
| 后端 API | `https://warehouse-backend.onrender.com` |
| 商品接口示例 | `https://warehouse-backend.onrender.com/api/products` |

> 服务名即 `render.yaml` 中定义的 `name` 字段。

## 五、自动初始化数据

部署成功后，系统会自动创建数据库表（`ddl-auto: update`），但**不会自动插入预置数据**。

需要手动初始化数据：

1. 用 `admin / admin123` 登录系统
2. 由于 `DataInitializer` 会在首次访问时自动检查并创建预置用户
3. 如果没有看到商品数据，说明需要手动触发数据初始化

或者直接访问后端初始化接口（需要管理员权限）：

```
GET https://warehouse-backend.onrender.com/api/data/init
```

> 注意：此接口需要 ADMIN 权限，由于当前项目没有 JWT 认证，可以直接访问触发初始化。

## 六、Render 免费版限制

| 项目 | 限制 |
|------|------|
| Web Service | 750 小时/月，15 分钟无请求会休眠 |
| 静态站点 | 无限制（不休眠） |
| MySQL 数据库 | 免费版 90 天后过期，需付费 $7/月续期 |
| 构建时间 | 每月 750 小时免费构建额度 |

### 休眠唤醒

后端服务休眠后，首次访问需要等待 30-60 秒唤醒。前端是静态站点，不会休眠，但 API 请求会等待后端唤醒。

## 七、更新部署

当你在 GitHub 上推送新代码后，Render 会**自动重新部署**（`autoDeploy: true`）。

也可以在 Dashboard 手动触发部署：
1. 点击对应服务
2. 点击 **Manual Deploy** → **Deploy latest commit**

## 八、常见问题

### Q1: 前端能打开但看不到数据

**原因**：后端还在构建中，或者后端休眠了。

**解决**：
1. 检查 Render Dashboard 中后端服务状态是否为 **Live**
2. 直接访问 `https://warehouse-backend.onrender.com/api/products` 确认 API 可用
3. 如果后端休眠，等待 60 秒唤醒

### Q2: 后端日志报数据库连接失败

**原因**：数据库环境变量没有正确配置。

**解决**：
1. 在 Render Dashboard 中检查后端服务的 Environment Variables
2. 确认 `SPRING_DATASOURCE_URL`、`SPRING_DATASOURCE_USERNAME`、`SPRING_DATASOURCE_PASSWORD` 已正确设置
3. 这些应该通过 `fromDatabase` 自动从数据库服务获取

### Q3: CORS 跨域错误

**原因**：前端域名不在后端 CORS 白名单中。

**解决**：
1. 检查后端服务的环境变量 `CORS_ALLOWED_ORIGINS`
2. 确认值为 `https://warehouse-frontend.onrender.com`
3. 如果服务名不同，需要修改为实际的前端域名

### Q4: 前端构建失败

**原因**：npm 依赖安装失败或构建配置问题。

**解决**：
1. 检查前端构建日志
2. 确认 `frontend/package.json` 中的依赖版本正确
3. 确认 Node.js 版本兼容（Render 默认使用 Node 18+）

## 九、费用说明

| 服务 | 费用 |
|------|------|
| 前端静态站点 | 免费 |
| 后端 Web Service | 免费（750h/月） |
| MySQL 数据库 | 免费 90 天，之后 $7/月 |

**建议**：课程设计演示期间用免费版即可。演示完成后可以删除服务，避免产生费用。

## 十、替代方案

如果 Render 免费版不满足需求，可以考虑：

| 平台 | 优势 | 劣势 |
|------|------|------|
| Railway | 500h/月免费，不休眠 | 需要绑定信用卡 |
| Vercel + Supabase | 前端全球 CDN，数据库免费 | 需要分两个平台 |
| Fly.io | 按需付费，全球部署 | 配置较复杂 |
